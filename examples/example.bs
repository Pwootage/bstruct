struct MySuper {
    i32 id
}

struct MyClass: MySuper {
    MyEnum status
}

enum MyEnum: u32 {
    a,
    b,
    c,
    d=7,
    e,
    f=0x10,
    g
}

struct A {
    size 20
    A a
}

struct vector<T> {
    *T first
    u32 size
    u32 capacity
}

struct FixedArray<T> {
    T[10] array
}

struct HasAVec {
    vector<i32> vec
}

struct HasAnArray {
    FixedArray<A> arr
}